/*백준 11021번 문제
  1. 두 정수 A와 B를 입력받고 A+B를 출력하는 프로그램
  2. 테스트 케이스 개수 T가 주어진다(각 테스트 케이스는 한줄로 이루어져있으며,
     각 줄에 A와 B가 주어진다.
  3. 각 테스트 케이스마다 "Case #x: "를 출력한 다음, A+B를 출력한다. 
  	 테스트 케이스 번호는 1부터 시작한다.  */

#include <stdio.h>

int main(){
	int  t, a, b, i;
	
	scanf("%d", &t);
	
	for(i = 1; i <= t; i++){
		scanf("%d %d", &a, &b);
		printf("Case #%d: %d\n", i, a+b);
	}
	
	
	return 0;
}
