//n이 주어졌을 때, 1부터 n까지 합을 구하는 프로그램
//처음 작성할 때 sum을 0으로 초기화 하지 않아 틀림(변수 초기화를 하지 않으면 쓰레기 값을 가지므로 의도치 않은 결과값이 나올 수 있음)

#include <stdio.h>

int main(){
  //입력할 변수 n과 1부터 n까지의 합을 저장할 sum 변수 선언
	int n, sum = 0; 

 //입력값을 변수 n에 저장
	scanf("%d", &n);

 //1부터 입력 받은 수의 합을 계산하는 for문
	for(int i = 1; i <= n; i++){
		sum+=i;
	}

 //결과 출력
	printf("%d", sum);
	
	return 0;
}
