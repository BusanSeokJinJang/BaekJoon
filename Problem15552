/*테스트 케이스 만큼 입력받고, a+b 값을 구하는 문제
  1. 테스트 케이스 만큼 입력받기 위해 for문을 사용하고 입력받을 input 변수를 선언
  2. for문 안에서 a와 b값을 입력받아 input 개수만큼 a와 b를 입력, 출력 */

#include <stdio.h>

int main(){
	int input, a, b, i;
	
	scanf("%d", &input);
	
	for(i = 0; i < input; i++){
		scanf("%d %d", &a, &b);
		printf("%d\n", a+b);
	}
	
	
	return 0;
}
